services:
  - type: web
    name: global-union-forum
    env: python
    buildCommand: pip install -r flask_registration/requirements.txt
    startCommand: cd flask_registration && gunicorn run:app
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0
